{
  "name": "agent-bus",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local-first control plane and universal harness for heterogeneous autonomous coding and research agents.",
  "engines": {"node": ">=22.5"},
  "bin": {
    "agent-bus": "./dist/cli.js",
    "agent-bus-mcp": "./dist/mcp-server.js",
    "agent-bus-openai-compatible": "./dist/openai-compatible-harness.js"
  },
  "scripts": {
    "build:core": "tsc",
    "build:web": "vite build",
    "build": "npm run build:core && npm run build:web",
    "dev": "vite --host 127.0.0.1",
    "broker": "node dist/cli.js broker",
    "install:global": "bash ./install.sh",
    "test:compile": "tsc -p tsconfig.test.json",
    "test:unit": "npm run test:compile && mkdir -p dist-test && cp agent-bus.config.json dist-test/agent-bus.config.json && node --test --test-force-exit dist-test/tests/*.test.js",
    "test:browser": "node scripts/browser-smoke.mjs",
    "test": "npm run build && npm run test:unit && npm run test:browser",
    "check": "npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "typescript": "^5.6.3",
    "vite": "^7.1.2"
  }
}
