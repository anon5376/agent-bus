{
  "name": "agent-bus",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local-first control plane and universal harness for heterogeneous autonomous coding and research agents.",
  "engines": {
    "node": ">=22.5"
  },
  "bin": {
    "agent-bus": "./dist/cli.js",
    "agent-bus-mcp": "./dist/mcp-server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "broker": "node dist/cli.js broker",
    "test:compile": "tsc -p tsconfig.test.json",
    "test": "npm run build && npm run test:compile && mkdir -p dist-test && cp agent-bus.config.json dist-test/agent-bus.config.json && node --test dist-test/tests/*.test.js",
    "check": "npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
